package Guia_academia.model;


public class Plano {
    private String nome;
    private String tipo;
    private boolean ativo;

    /*Um plano tem nome ("Plano Ouro"), tipo ("Mensal") e um status ativo (true/false).*/

    public Plano(String nome, String tipo, boolean ativo) {
        this.nome = nome;
        this.tipo = tipo;
        this.ativo = ativo;
    }

    public String getNome() { return nome; }
    public String getTipo() { return tipo; }
    public boolean isAtivo() { return ativo; }
}

/*Criamos o plano com nome, tipo e ativo. Os métodos permitem acessar os dados do plano.

 */

